# Copy to .env in this directory. Expo auto-loads it for the mobile app.
# Note the EXPO_PUBLIC_ prefix — required for Expo to expose the var to RN.
# The URL comes from your Convex deployment (see /convex/README.md).
EXPO_PUBLIC_CONVEX_URL=
